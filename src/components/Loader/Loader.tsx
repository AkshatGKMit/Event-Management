import "./Loader.scss";

const Loader = () => {
    return (
        <div className="loader-wrapper">
            <div className="loader-icon"></div>
            <label className="loader-label"> Loading... </label>
        </div>
    );
};

export default Loader;
